# Catalogue metadata the bench reads when publishing this set.
stack = ["postgresql"]
level = "advanced"
blurb = "Read a query plan and say why it is slow; add the index that changes it, and see what two concurrent sessions do to a balance."
position = 4
