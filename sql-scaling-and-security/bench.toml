# Catalogue metadata the bench reads when publishing this set.
stack = ["postgresql"]
level = "advanced"
blurb = "Read roles, injections, prepared statements, a replica that lags and a shard that runs hot, and say which one your system has."
position = 5
